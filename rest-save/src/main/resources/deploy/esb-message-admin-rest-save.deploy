{ "deployments" => 
    { "${project.build.finalName}.${project.packaging}" => 
        {
            "NAME" => "${project.build.finalName}.${project.packaging}",
            "path" => "/var/lib/jbossas/standalone/deployments/${project.build.finalName}.${project.packaging}",
            "replace-name-regex" => "\\Q${project.artifactId}\\E-${version.major}\\..*",
        }
   }
}